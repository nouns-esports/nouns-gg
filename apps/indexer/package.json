{
  "name": "indexer",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun --env-file=../../.env.development ponder dev --schema=indexer-test",
    "start": "ponder start --schema=$RAILWAY_DEPLOYMENT_ID"
  }
}
