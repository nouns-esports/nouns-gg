export const channels: string[] = [];

export async function getChannels() {
	return [];
}

export async function getChannel(_input: { channel: string }) {
	return undefined;
}

export async function getFeed(_input: { channel: string }) {
	return [];
}
