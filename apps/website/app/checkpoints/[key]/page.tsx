import ArchiveNotice from "@/components/ArchiveNotice";

export default function Checkpoint() {
	return (
		<ArchiveNotice
			title="Checkpoint unavailable"
			description="This checkpoint is no longer available."
		/>
	);
}
