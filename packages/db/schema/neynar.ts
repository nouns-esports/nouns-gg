// https://docs.dune.com/data-catalog/community/farcaster/casts

// import { pgTable, bigint, timestamp, text, varchar, jsonb, bytea } from "drizzle-orm/pg-core";

// export const dataset_farcaster_casts = pgTable("dataset_farcaster_casts", {
// 	id: bigint("id").primaryKey(),
// 	created_at: timestamp("created_at", { mode: "string" }),
// 	updated_at: timestamp("updated_at", { mode: "string" }),
// 	deleted_at: timestamp("deleted_at", { mode: "string" }),
// 	timestamp: timestamp("timestamp", { mode: "string" }),
// 	fid: bigint("fid"),
// 	hash: bytea("hash"),
// 	parent_hash: bytea("parent_hash"),
// 	parent_fid: bigint("parent_fid"),
// 	parent_url: varchar("parent_url"),
// 	text: text("text"),
// 	embeds: jsonb("embeds"),
// 	mentions: bigint("mentions").array(),
// 	mentions_positions: bigint("mentions_positions", { mode: "number" }).array(),
// 	root_parent_hash: bytea("root_parent_hash")
// });

